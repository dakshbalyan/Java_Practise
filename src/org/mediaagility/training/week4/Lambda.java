package org.mediaagility.training.week4;

public class Lambda {
    public static void main(String[] args) {

        // Running a simple lambda expression through a thread
        /* Runnable variable = () -> System.out.println("hello world !");
        Thread thread = new Thread(variable);
        thread.start(); */

//        int var = ()-> 2;
    // It is invalid to specify the return keyword when you have a one liner lambda
    // expression without {}!

        // You execute a lambda expression by calling the interface method on it just as
        // if it was an instance of the class
    }
}
